name: benchmark-deploy-debug

on:
  workflow_dispatch:

jobs:
  benchmark-deploy-debug:
    uses: ./.github/actions/ci-benchmark-deploy-debug.yml@main
    secrets: inherit
    with:
      client-vm: benchmark-gatling
      frontend-vm: benchmark-frontend
      backend-vm: benchmark-backend
